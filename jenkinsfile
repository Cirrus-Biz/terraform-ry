pipeline {
    agent any
    stages {
        stage('Terraform Init') {
            steps {
                sh 'terraform init' 
            }   
        }
        stage('Terraform Plan') {
            withCredentials([file(credentailsId: 'ry-terraform-vars', variable: 'ry-tfvars')])
            steps {
                sh 'cat $ry-tfvars'
            }
        }
    }
}